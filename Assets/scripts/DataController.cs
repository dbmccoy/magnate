using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using System.Linq;
using System.IO;

public class DataController : MonoBehaviour {

    private string LotZoningData = "data/ZoningData.json";

    // Use this for initialization
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }

    void LoadJson()
    {
        //string filePath = Path.Combine(Application.streamingAssetPath, gameDataFileName);
        //string dataAsJson = File.ReadAllText(filePath);
        
    }
}
